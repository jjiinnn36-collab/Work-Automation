# ── 사내 업무 정보 — 외부 저장소에 올리지 않는다 ────────────
data/payment-master.tsv
data/status.tsv
data/run.log
data/apikey.txt

# 엑셀 원본 (기관·기한·금액 포함)
*.xlsx
~$*
# 단, 입력 양식은 금액이 비어 있으므로 공유한다
!data/payment-master-template.xlsx

# 인터뷰 명세 (납부 항목 전체가 들어 있음)
.omc/

# ── 빌드 산출물 ──────────────────────────────────────────
*.exe
*.pdb
build/

# ── 진단 결과 ────────────────────────────────────────────
env-check-result.txt
