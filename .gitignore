# ── 사내 업무 정보 — 외부 저장소에 올리지 않는다 ────────────
data/payment-master.tsv
data/amounts.tsv
data/attachments.tsv
data/board-position.txt
data/start-date.txt

# 첨부한 증빙 원본 (신고서·영수증 등)
증빙/
data/status.tsv
data/run.log
data/apikey.txt

# 자료 DB — 납부 항목·금액·진행 기록이 전부 한 파일에 들어 있다
*.db
*.db-wal
*.db-shm
*.db-journal
*.importing
# 자료 폴더 위치 설정 (PC 마다 다름)
data-folder.txt

# 엑셀 원본 (기관·기한·금액 포함)
*.xlsx
~$*
# 단, 입력 양식은 금액이 비어 있으므로 공유한다
!data/payment-master-template.xlsx

# 인터뷰 명세 (납부 항목 전체가 들어 있음)
.omc/

# 납부 관련 원본 문서 — 사업자등록번호·가상계좌번호·납부금액 포함
비용납부내역/
*.pdf

# ── 빌드 산출물 ──────────────────────────────────────────
*.exe
*.pdb
build/

# 화면 시안 (실제 금액 포함)
mockup-web.html

# ── 진단 결과 ────────────────────────────────────────────
env-check-result.txt

# 화면 시안 모음 (실제 금액 포함)
mockup-web-*.html
design-alert-web/
design-canvas/
design_*.md
