# 사용자 결정 기록

자율 작업 중 사용자 판단이 필요했던 항목과 결정 결과. 결정일 2026-09-16.
**보류** 항목은 지금 동작을 그대로 두고, 결정하면 이 문서에 결과를 적고 구현한다.

| # | 질문 | 결정 | 반영 |
|---|---|---|---|
| Q1 | 추적 시작일 이전 건을 화면에 남길지 | **지난 건에 흐리게 남긴다** | 완료 — ADR-0014 |
| Q2 | 당월 보드 창을 둘지 | **보류** | 지금대로 유지 |
| Q3 | 이번 달 집계 낱말 | **연간과 같게 (진행중 = 한 단계라도 밟음 / 진행예정 = 아직 안 밟음)** | 완료 — ADR-0014 |
| Q4 | 웹 첫 화면 | **받은 알림** | 지금대로 |
| Q5 | 공개 저장소의 실제 금액 | **GitHub 에 올리는 파일에서는 금융투자협회비 금액을 가짜 값으로** | 완료 — 테스트 값 교체 (아래 참고) |
| Q6 | 팝업 오른쪽 여백 | **지금대로 12px** | 지금대로 |
| Q7 | 백업 보관 개수·위치 | **보류** | 지금대로 (자료 폴더 안, 30개, `backup-folder.txt` 로 변경 가능) |
| Q8 | 스캔 문서 판독 | **판독 기능 자체를 없앤다. 금액은 직접 입력** | 완료 — ADR-0012 개정 |
| Q9 | `jbam-web` 원래 git 기록 | **이름만 바꿔 보관** | 완료 — `jbam-web/.git-template-backup` (저장소 제외) |
| Q10 | 여러 항목 한꺼번에 넣기 | **지금대로** (웹에서 한 건씩, 분할납부는 월 쉼표) | 지금대로 |
| Q11 | GitHub 에 올리기 | **올린다** | 완료 — `git push origin main` |

## Q5 참고

- 테스트 코드(`test/*.cs`, `jbam-web/lib/logic.test.ts`)의 협회비 금액을 가짜 값(1,234,560 / 1,234,558)으로 바꿨다.
- **이전 커밋 기록에는 옛 값이 남아 있다.** 기록까지 지우려면 기록을 고쳐 쓰고 강제로 올려야 해서(되돌릴 수 없음) 하지 않았다.
  필요하면 따로 요청한다.

## 보류 항목의 지금 동작

### Q2. 당월 보드
- `start-board.bat` / `install-board.bat` 로 띄우는 작은 창이 그대로 있다. 자료·단계 이름·금액 규칙은 웹과 같은 코드를 쓴다.
- 폐기하면 `MonthlyBoard.cs`, 두 bat 을 지우고 웹 '이번 달' 로 대체한다.

### Q7. 백업
- 하루 첫 실행에 `자료폴더\backups\납부알림-YYYY-MM-DD.db`, 날짜별 사본 최근 30개.
- 다른 디스크에 두려면 실행 파일 옆 `backup-folder.txt` 에 경로 한 줄.
